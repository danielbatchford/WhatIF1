<sf:ChromelessWindow
    x:Class="WhatIfF1.UI.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WhatIfF1"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sf="http://schemas.syncfusion.com/wpf"
    xmlns:sfskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
    sfskin:SfSkinManager.Theme="{sfskin:SkinManagerExtension ThemeName=MaterialDark,
                                                             FluentHoverEffectMode=BackgroundAndBorder,
                                                             FluentPressedEffectMode=Reveal}"
    Icon="Icons/favicon.png"
    KeyDown="MainWindow_KeyDown"
    Loaded="MainWindow_Loaded"
    ResizeBorderThickness="0"
    ResizeMode="CanResizeWithGrip"
    ShowIcon="False"
    TitleBarBackground="{StaticResource mainColor}"
    TitleBarForeground="White"
    UseNativeChrome="True"
    mc:Ignorable="d">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Grid
            Grid.Row="0"
            Grid.Column="0"
            DataContext="{Binding ActiveScenario}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
                <RowDefinition Height="2" />
            </Grid.RowDefinitions>

            <Grid
                Grid.Row="0"
                Grid.Column="0"
                Visibility="{Binding Path=., Converter={StaticResource isNotNullToVisConverter}}">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>

                <Image
                    Grid.Row="0"
                    Grid.Column="0"
                    Width="130"
                    Margin="10,10,0,10"
                    HorizontalAlignment="Left"
                    Source="{Binding Track.FlagFilePath}" />

                <Grid Grid.Row="0" Grid.Column="1">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="0"
                        Margin="10,5,0,0"
                        FontFamily="{StaticResource boldFont}"
                        FontSize="30"
                        Text="{Binding EventName}" />

                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="0"
                        Margin="10,0,0,0"
                        FontFamily="{StaticResource regularFont}"
                        FontSize="20"
                        Text="{Binding Track.TrackName}" />

                    <Grid Grid.Row="2" Grid.Column="0">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*" />
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="0"
                            Margin="10,5,0,0"
                            FontFamily="{StaticResource boldFont}"
                            FontSize="15"
                            FontStyle="Italic"
                            Text="{Binding EventDate.Year}" />

                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="0,5,0,0"
                            FontFamily="{StaticResource regularFont}"
                            FontSize="15"
                            Text="  |  " />

                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="2"
                            Margin="0,5,0,0"
                            FontFamily="{StaticResource regularFont}"
                            FontSize="15"
                            Text="{Binding Track.LocationName}" />

                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="3"
                            Margin="0,5,0,0"
                            FontFamily="{StaticResource regularFont}"
                            FontSize="15"
                            Text="  |  Round" />


                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="4"
                            Margin="5"
                            FontFamily="{StaticResource boldFont}"
                            FontSize="15"
                            FontStyle="Italic"
                            Text="{Binding Round}" />

                    </Grid>

                </Grid>

                <Grid
                    Grid.Row="0"
                    Grid.Column="2"
                    Height="50"
                    Margin="0,0,10,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150" />
                        <ColumnDefinition Width="150" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="50" />
                    </Grid.RowDefinitions>


                    <Button
                        Grid.Row="0"
                        Grid.Column="0"
                        Margin="{StaticResource defaultMargin}"
                        Background="{StaticResource mainColor}"
                        Command="{Binding AboutRaceCommand}"
                        Content="About Race"
                        FontFamily="{StaticResource boldFont}"
                        FontSize="15"
                        IsEnabled="{Binding WikipediaLink, Converter={StaticResource isNotNullConverter}}" />


                    <Button
                        Grid.Row="0"
                        Grid.Column="1"
                        Margin="{StaticResource defaultMargin}"
                        Background="{StaticResource mainColor}"
                        Command="{Binding LoadRaceCommand}"
                        Content="Load Race"
                        FontFamily="{StaticResource boldFont}"
                        FontSize="15"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource inverseBoolConverter}}" />

                </Grid>

            </Grid>

            <Rectangle
                Grid.Row="1"
                Grid.Column="0"
                Height="2"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Bottom"
                Fill="{Binding Color, UpdateSourceTrigger=PropertyChanged, FallbackValue={StaticResource mainColor}, Converter={StaticResource colorToBrushConverter}}" />

        </Grid>

        <Grid Grid.Row="1" Grid.Column="0">

            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" MinWidth="250" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="1*" MinWidth="400" />
            </Grid.ColumnDefinitions>

            <sf:SfAccordion
                Grid.Row="0"
                Grid.Column="0"
                HorizontalAlignment="Stretch"
                SelectionMode="OneOrMore">

                <sf:SfAccordion.HeaderTemplate>
                    <DataTemplate>
                        <TextBlock
                            FontFamily="{StaticResource boldFont}"
                            Foreground="White"
                            Text="{Binding}" />
                    </DataTemplate>
                </sf:SfAccordion.HeaderTemplate>


                <sf:SfAccordionItem Header="Browse Scenarios">
                    <ListView
                        x:Name="ScenarioListView"
                        HorizontalContentAlignment="Stretch"
                        ItemTemplate="{StaticResource scenarioItemTemplate}"
                        ItemsSource="{Binding Scenarios}"
                        SelectedItem="{Binding ActiveScenario, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
                </sf:SfAccordionItem>

            </sf:SfAccordion>

            <sf:SfGridSplitter
                Grid.Row="0"
                Grid.Column="1"
                Width="10"
                HorizontalAlignment="Stretch"
                EnableCollapseButton="True"
                ResizeBehavior="PreviousAndNext" />

            <Grid
                x:Name="ScenarioGrid"
                DataContext="{Binding ActiveScenario, UpdateSourceTrigger=PropertyChanged}"
                IsEnabled="{Binding ActiveScenario, Converter={StaticResource isNotNullConverter}}" />

        </Grid>

        <Rectangle
            Grid.Row="2"
            Grid.Column="0"
            Height="2"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Top"
            Fill="{StaticResource mainColor}" />
        <Grid
            x:Name="LogBarGrid"
            Grid.Row="2"
            Grid.Column="0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="{Binding CurrentColor, UpdateSourceTrigger=PropertyChanged, FallbackValue=LightGray, Converter={StaticResource colorToBrushConverter}}">

            <TextBlock
                Margin="{StaticResource defaultMargin}"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Text="{Binding CurrentMessage, UpdateSourceTrigger=PropertyChanged}" />

        </Grid>

    </Grid>



</sf:ChromelessWindow>
