<Application
    x:Class="WhatIfF1.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:WhatIfF1.UI.Converters"
    xmlns:local="clr-namespace:WhatIfF1"
    xmlns:sf="http://schemas.syncfusion.com/wpf"
    DispatcherUnhandledException="Application_DispatcherUnhandledException"
    Exit="Application_Exit"
    ShutdownMode="OnMainWindowClose"
    StartupUri="UI/Windows/MainWindow.xaml">
    <Application.Resources>

        <converters:IsNotNullConverter x:Key="isNotNullConverter" />
        <converters:ColorToBrushConverter x:Key="colorToBrushConverter" />

        <SolidColorBrush x:Key="mainColor" Color="Firebrick" />

        <Thickness
            x:Key="defaultMargin"
            Bottom="5"
            Left="5"
            Right="5"
            Top="5" />

        <DataTemplate x:Key="scenarioItemTemplate">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Grid.Column="0"
                    Margin="5"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Text="{Binding EventCode}" />

                <Rectangle
                    Grid.Row="0"
                    Grid.Column="0"
                    Height="2"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Bottom"
                    Fill="{Binding Color, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource colorToBrushConverter}}" />
            </Grid>
        </DataTemplate>

    </Application.Resources>
</Application>
